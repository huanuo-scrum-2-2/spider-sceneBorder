一.Git操作：
    1.首先安装git，建立git账户，并及时将账户名发群里，方便添加权限
    2.在自己的目录（最好是新建一个目录）下执行git clone https://github.com/xiongyuqiong/spider-buildingBorder.git  即可将初始代码下载到本地
    3.修改代码前先pull线上的代码，修改完后及时commit and push代码   -- 此项一定要及时，避免冲突
    4.其他问题可百度或者在群里询问

二.代码规范
    1.每个页面对应views下的一个文件夹（假设名字问name），每个人在新建页面的时候，请提前在群里告知，避免命名重复，且名字尽量用相应功能的名字，方便他人理解，
      每个页面必须包含且只包含name.js,name.css,name.html三个文件，其他资源文件不存放在此文件夹,可参考login目录。
    2.static里已经引入的库包括echarts,juqery，有需要的页面直接引入这两个文件，不要直接应用cdn或者其他版本的文件.
      若有人需要引入其他的库，务必在群里先告知其他成员，再进行引入，不要重复引入不同版本的库。
    3.所有的图片文件必须统一放到static/images文件夹下（无需新建文件夹，除非某个页面大量引入图片，可自行新建文件夹），
      图片最好都为png格式，且图片不能太大（所实在太大自行压缩)
    4.在调试个人页面的时候，可将index.html中menu_ul中<a>标签对应的herf改为自己页面的地址，即可在首页进行跳转。
    5.每个人成员目前无需考虑页面美化，此文档的主要目的为规范内容，大家调整之前库的引入地址和图片等资源文件的引用地址。务必满足以上所说的结构。